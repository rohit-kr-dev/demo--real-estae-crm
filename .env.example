# Local development defaults to http://localhost:5000 automatically.
# Set both values in Vercel only when connecting a public HTTPS backend.
# Leave them unset for the frontend-only demo mode.
VITE_API_BASE_URL=https://your-crm-api.example.com/api
VITE_SOCKET_URL=https://your-crm-api.example.com
